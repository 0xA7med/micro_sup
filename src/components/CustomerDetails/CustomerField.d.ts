import React from 'react';
interface CustomerFieldProps {
    label: string;
    children: React.ReactNode;
}
export default function CustomerField({ label, children }: CustomerFieldProps): import("react/jsx-runtime").JSX.Element;
export {};
