import React from 'react';
interface FormFieldProps {
    label: string;
    children: React.ReactNode;
    fullWidth?: boolean;
}
export default function FormField({ label, children, fullWidth }: FormFieldProps): import("react/jsx-runtime").JSX.Element;
export {};
