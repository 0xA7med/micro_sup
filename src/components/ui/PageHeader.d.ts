import React from 'react';
interface PageHeaderProps {
    title: string;
    backUrl?: string;
    actions?: React.ReactNode;
}
export default function PageHeader({ title, backUrl, actions }: PageHeaderProps): import("react/jsx-runtime").JSX.Element;
export {};
